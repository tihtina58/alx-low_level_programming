#include "holberton.h"

/**
 * main - check alphabetic character
 *
 * Return: 1 if its letter otherwise 0
 */
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
