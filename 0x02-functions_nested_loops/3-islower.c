#include "holberton.h"

/**
 * main - check the code for the ALX school students
 * @c: integer to be tested
 * Return: Always 0
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
